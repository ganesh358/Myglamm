import React from 'react';
// import { useSelector } from 'react-redux';
// import init from '../reducer/AuthReducer/authReducer';

const Dashcheck = () => {

    // const {isAuth}=useSelector((state)=>state.auth);

  
  return (
<div>Dashcheck</div>

 )
}

export default Dashcheck